# HeyBar v1.1.2

## Fixes

- Fixed a relaunch issue where HeyBar could reopen without showing its menu bar item after the app had been fully quit.
- Fixed the Settings helper lifecycle so closing Settings no longer leaves a hidden helper process behind to interfere with the next app launch.
